# Dockerfile para desarrollo con hot-reload
FROM node:22-alpine

# Establecer el directorio de trabajo
WORKDIR /app

# Instalar herramientas necesarias
RUN apk add --no-cache git

# Exponer el puerto 3000
EXPOSE 3000

# El comando se ejecutará desde docker-compose.dev.yml
# Esto permite que npm install se ejecute cuando el contenedor inicia
# con acceso al volumen persistente de node_modules
CMD ["npm", "start"]
